repos:
  - repo: local
    hooks:
      - id: cargo-sort
        name: Check Cargo.toml is sorted
        description: 'Ensure Cargo.toml is sorted'
        entry: cargo-sort -w
        language: rust
        types: [file, toml]
        files: Cargo\.toml
        pass_filenames: false

